export interface IFsAddressFormatConfig {
  name?: boolean,
  country?: boolean,
  region?: boolean,
  city?: boolean,
  street?: boolean,
  zip?: boolean,
  isShort?: boolean,
  linesCount?: number
}
